import type { NextPage } from "next";
import { Layout } from "components";

const index: NextPage = () => {
  return (
    <Layout>
      <p>Hello, World!</p>
    </Layout>
  );
};

export default index;
